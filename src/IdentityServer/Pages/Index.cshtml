@page
@model OidcDemo.Services.Identity.Pages.IndexModel
@{
    ViewData["Title"] = "Index";
}

<dl>
    @foreach (var claim in User.Claims)
    {
        <dt>@claim.Type</dt>
        <dd>@claim.Value</dd>
    }
</dl>
